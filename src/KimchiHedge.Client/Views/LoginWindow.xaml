<Window x:Class="KimchiHedge.Client.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="KimchiHedge - 로그인"
        Height="500" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundDarkestBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 메인 로그인 영역 -->
        <Border Grid.Row="0"
                Background="{StaticResource BackgroundDarkBrush}"
                Margin="32"
                Padding="32"
                CornerRadius="4"
                BorderBrush="{StaticResource BorderDefaultBrush}"
                BorderThickness="1">
            <StackPanel VerticalAlignment="Center">
                <!-- 로고 -->
                <TextBlock Text="KimchiHedge"
                           Style="{StaticResource TextBlockHeader}"
                           Foreground="{StaticResource AccentBlueBrush}"
                           HorizontalAlignment="Center"
                           FontSize="{StaticResource FontSize2XL}"/>
                <TextBlock Text="김치프리미엄 헷지 자동매매"
                           Style="{StaticResource TextBlockMuted}"
                           HorizontalAlignment="Center"
                           Margin="0,8,0,32"/>

                <!-- 아이디 입력 -->
                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="아이디"
                               Style="{StaticResource TextBlockLabel}"
                               Margin="0,0,0,8"/>
                    <TextBox x:Name="EmailTextBox"
                             Style="{StaticResource TextBoxLine}"
                             Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                             IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}"
                             Height="36"/>
                </StackPanel>

                <!-- 비밀번호 입력 -->
                <StackPanel Margin="0,0,0,24">
                    <TextBlock Text="비밀번호"
                               Style="{StaticResource TextBlockLabel}"
                               Margin="0,0,0,8"/>
                    <PasswordBox x:Name="PasswordBox"
                                 Style="{StaticResource PasswordBoxLine}"
                                 IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}"
                                 Height="36"/>
                </StackPanel>

                <!-- 에러 메시지 -->
                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="{StaticResource StatusLossBrush}"
                           FontSize="{StaticResource FontSizeSM}"
                           TextWrapping="Wrap"
                           Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}"
                           Margin="0,0,0,16"/>

                <!-- 로그인 버튼 -->
                <Button Content="로그인"
                        Style="{StaticResource ButtonPrimary}"
                        Height="44"
                        Command="{Binding LoginCommand}"
                        CommandParameter="{Binding ElementName=PasswordBox}"
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}"/>

                <!-- 로딩 표시 -->
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"
                            Margin="0,16,0,0">
                    <TextBlock Text="로그인 중..."
                               Style="{StaticResource TextBlockMuted}"/>
                </StackPanel>

                <!-- 회원가입 링크 -->
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Margin="0,20,0,0">
                    <TextBlock Text="계정이 없으신가요? "
                               Style="{StaticResource TextBlockMuted}"/>
                    <TextBlock>
                        <Hyperlink Click="OnRegisterLinkClick"
                                   Foreground="{StaticResource AccentBlueBrush}">
                            회원가입
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- 하단 정보 -->
        <StackPanel Grid.Row="1"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,16">
            <TextBlock Text="v1.0.0"
                       Style="{StaticResource TextBlockMuted}"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
